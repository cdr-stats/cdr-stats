

~~~ TODO List ~~~
    
 - http://localhost:8000/show_stats_by_hour/
    the select date look is not the same on the Search CDR, the table have some spacing
    
 - on show_stats_by_day/ and show_stats_by_month/
  no need to display the filter on a second lines,
  For instance we can display DESTINATION [------------] (Select for Destination type)
  
 - Select language, no need to have a GO button, just sumbit the language when the user select a new language
   
 
